# TicTacToe_Terminal_Game
Method called game lets player to decide play or not.if he or she doesn't program stop working.If the answer isn't like what we exepected,it prints out about answer's invalidity.On the other hand,if player wants to play, a loop starts and it's length is 9 because in the game you would choose 9 location.In the game first player is X and second is O.So, we check the loop variable if it does divideable by 2,we know that X is located.If it doesn't,we know that O is located.We use 3*3 matrix to keep the loaction which is chose by player.The information is given the user by printing who'se playing.To prevent player's risk of choosing a location which is not free,I checked that if lacotion is free or not in a loop.If it's free we locate it than break the loop.I make sure that player can't choose a number is invalid for row or column.For invalid variables I printed the information.The information is you need to know,first we fill the matrix then check.Each turn we see the changes in matrix.Some other methods is called from now on in this method.
control_the_list method checks possible ways of winning.I relaised when I started the program sometimes both players can win cause I fill the matrix before I check.So I put the results or result into a list.We should know the winner to print out this information.If list's length is greater than 1 it means that winner is both players.if it doesn't we reach it with boolean value we set in ifs.If boolean value is true it returns player.Other ways,it returns none.
print_result method if player doesn't none prints player else it prints the message like this We don't have any winner nor loser
delete_everything method makes the list like it never used.It changes the members with this - so list is clear.
main method is for list that use in this should be always the same so I wrote it in main and called game in it than I called main
****
Maybe,it is a bit complicated but it was more easier for me to do it this way.I use loops a lot so it has complexity
